/**
 * Top-level routes utilities and components.
 * - createAppRouter: Factory for the app's React Router instance.
 * - RouterFallback: Suspense fallback used during lazy route loading.
 */
export { default as createAppRouter } from './createAppRouter';
export { default as RouterFallback } from './RouterFallback';
